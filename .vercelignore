# Anchored to the repo root. Without the leading slash these patterns match at
# any depth, and a bare `data` would also strip src/data — the generated
# geometry and statistics the pages cannot render without.
node_modules
package-lock.json
/data
/tools
/README.md
